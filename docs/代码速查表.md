> **注：** 本文档由 **claude-sonnet-4-6** 模型自动生成。

# kiro2cc-proxy 代码索引

> 功能 → 代码位置速查表，供 Claude Code 快速定位使用。

---

## 目录

1. [请求链路总览](#请求链路总览)
2. [src/main.rs](#1-srcmainrs)
3. [src/kiro/token_manager.rs](#2-srckirotokenmanagerrs)
4. [src/kiro/provider.rs](#3-srckiroproviderrs)
5. [src/kiro/machine_id.rs](#4-srckiromachineidrs)
6. [src/kiro/parser/](#5-srckiroparsers)
7. [src/kiro/model/](#6-srckiromodel)
8. [src/anthropic/router.rs](#7-srcanthropicrouterrs)
9. [src/anthropic/handlers.rs](#8-srcanthropichandlersrs)
10. [src/anthropic/converter.rs](#9-srcanthropicconverterrs)
11. [src/anthropic/stream.rs](#10-srcanthropicstreamrs)
12. [src/anthropic/middleware.rs](#11-srcanthropicmiddlewarers)
13. [src/anthropic/types.rs](#12-srcanthropictypesrs)
14. [src/anthropic/websearch.rs](#13-srcanthropicwebsearchrs)
15. [src/model/config.rs](#14-srcmodelconfigrs)
16. [src/model/rpm.rs](#15-srcmodelrpmrs)
17. [src/model/throttle_log.rs](#16-srcmodelthrottlelogrs)
18. [src/model/api_key.rs](#17-srcmodelapikeyrs)
19. [src/model/usage.rs](#18-srcmodelusagers)
20. [src/model/arg.rs](#19-srcmodelargrs)
21. [src/model/failure_log.rs](#20-srcmodelfailurelogrs)
22. [src/cache/](#21-srccache)
23. [src/admin/](#22-srcadmin)
24. [src/admin_ui/](#23-srcadminui)
25. [src/user/](#24-srcuser)
26. [src/user_ui/](#25-srcuserui)
27. [src/common/auth.rs](#26-srccommonauthrs)
28. [src/http_client.rs](#27-srchttpclientrs)
29. [src/token.rs](#28-srctokenrs)
30. [src/debug.rs](#29-srcdebugrs)
31. [src/log_capture.rs](#30-srclogcapturers)

---

## 请求链路总览

```
Client (Anthropic format)
  │
  ▼
src/anthropic/middleware.rs   auth_middleware         认证、AppState
  │
src/anthropic/router.rs       build_router            路由注册（/v1、/cc/v1）
src/anthropic/handlers.rs     post_messages           路由入口
  │                           post_messages_cc        缓冲模式(/cc/v1)
src/anthropic/converter.rs    convert_request         Anthropic→Kiro 格式转换
  │
src/kiro/provider.rs          call_api_stream         HTTP 调用 + 重试/故障转移
  │                           call_api_with_retry     429/401/402/5xx 处理
src/kiro/token_manager.rs     acquire_context         账号选择 + token 刷新
  │
  ▼  (Kiro 二进制帧协议)
src/kiro/parser/decoder.rs    EventStreamDecoder      流式帧解码
src/kiro/parser/frame.rs      parse_frame             单帧 CRC 验证 + 解析
  │
src/anthropic/stream.rs       SseStateManager         Kiro 事件→Anthropic SSE
src/cache/mod.rs               select_final_usage      四层降级链确定 prompt cache usage
  ▼
Client (Anthropic SSE format)
```

---

## 1. `src/main.rs`

**职责**：应用入口，加载配置/凭证，组装所有组件，启动 HTTP 服务。

| 内容 | 行号 |
|------|------|
| `main()` tokio 异步入口 | 29–296 |
| 加载 `Config` | 53 |
| 加载 `CredentialsConfig` | 66 |
| 构建 `MultiTokenManager` | 103 |
| 构建 `RpmTracker` | 117 |
| 构建 `ThrottleLogStore` | 124 |
| 构建 `KiroProvider`（`with_proxy`） | 141 |
| 初始化 `token::init_config` | 147 |
| 条件启用 Admin API / Admin UI / User API / User UI 路由 | 233–256 |
| `axum::serve` TCP 监听 | 289–290 |

---

## 2. `src/kiro/token_manager.rs`

**职责**：多账号 Token 生命周期管理（刷新、故障转移、负载均衡、统计持久化、Admin 操作）。

> 文件顶部还定义了一个未被使用的旧版 `struct TokenManager`（`#[allow(dead_code)]`，第 7–35 行左右），与本节描述的 `MultiTokenManager` 无关，属于预置死代码。

### 关键类型

| 类型 | 行号 | 说明 |
|------|------|------|
| `struct CredentialEntry` | 432 | 单账号运行时状态（内部） |
| `enum DisabledReason` | 461 | Manual / TooManyFailures / QuotaExceeded |
| `pub enum HealthStatus` | 473 | Healthy / Warning / Degraded / Unhealthy / Disabled |
| `struct StatsEntry` | 513 | 统计持久化结构（含 `last_throttled_wall`） |
| `pub struct CredentialEntrySnapshot` | 529 | Admin API 只读快照 |
| `pub struct ManagerSnapshot` | 568 | 全量快照（entries + current_id + available） |
| `pub struct MultiTokenManager` | 583 | 多账号管理核心（Mutex 包裹 entries） |
| `pub struct CallContext` | 662 | 一次调用绑定的账号 ID + token |

### 关键常量

| 常量 | 行号 | 值 |
|------|------|-----|
| `MAX_FAILURES_PER_CREDENTIAL` | 615 | 3 |
| `STATS_SAVE_DEBOUNCE` | 617 | 30s |
| `STICKY_CACHE_TTL` | 619 | 60min |
| `TOKEN_REFRESH_COOLDOWN` | 621 | 30s（两次刷新尝试之间的最小间隔，防止并发重复刷新） |

> 旧的 `HEALTH_WINDOW_DAYS`（4 天时间窗口）常量已不存在，`compute_health` 现在按“最近一次限流的相对耗时”判定，见下方说明。

### 关键函数

| 函数 | 行号 | 说明 |
|------|------|------|
| `MultiTokenManager::new` | 680 | 构造，分配 ID、加载统计 |
| `select_next_credential` | 828 | priority/balanced 模式选账号 |
| `acquire_context` | 926 | 获取可用账号 + token，含自愈 |
| `acquire_context_filtered` | 1022 | 带白名单的 acquire_context |
| `acquire_context_sticky` | 1068 | agentContinuationId sticky 路由 |
| `try_ensure_token` | 1215 | 双重检查锁 token 刷新（受 `TOKEN_REFRESH_COOLDOWN` 节流） |
| `persist_credentials` | 1309 | 原子写回凭证文件 |
| `compute_health` | 1476 | 计算 HealthStatus（见下方算法说明） |
| `report_throttled` | 1520 | 记录 429，写 `last_throttled_wall` |
| `report_throttled_for_rotation` | 1536 | 记录 429 但不计入失败计数，仅用于触发账号轮换 |
| `report_success` | 1550 | 重置失败计数，更新 last_used_at |
| `report_failure` | 1575 | 累加失败，达阈值自动禁用 |
| `report_quota_exhausted` | 1636 | 立即禁用额度耗尽账号 |
| `snapshot` | 1725 | 生成 ManagerSnapshot |
| `set_disabled` | 1765 | Admin：启用/禁用 |
| `set_priority` | 1790 | Admin：修改优先级 |
| `reset_and_enable` | 1807 | Admin：重置失败计数 |
| `get_usage_limits_for` | 1824 | Admin：查单账号余额 |
| `add_credential` | 1953 | Admin：新增账号 |
| `update_credential` | 2059 | Admin：更新账号字段 |
| `delete_credential` | 2253 | Admin：删除账号 |
| `set_load_balancing_mode` | 2329 | Admin：切换 priority/balanced |

### `compute_health` 算法（第 1476 行起，非时间窗口模型）

```
disabled                                       → Disabled
failure_count >= 3 / 2 / 1                     → Unhealthy / Degraded / Warning
total_calls(success+throttle) < 5              → Healthy（样本不足，暂不判定）
否则计算 throttle_rate = throttle_count / total_calls：
  距上次限流 < 120s 且 throttle_rate > 0.5      → Unhealthy
  距上次限流 < 600s 且 throttle_rate > 0.3      → Degraded
  距上次限流 < 600s 且 throttle_rate > 0.15     → Warning
  否则                                          → Healthy
```

---

## 3. `src/kiro/provider.rs`

**职责**：封装对 Kiro AWS API 的 HTTP 调用，多账号重试/故障转移，全局+按账号双重并发限流，sticky 路由。

### 关键类型

| 类型 | 行号 | 说明 |
|------|------|------|
| `pub struct KiroProvider` | 43 | 核心 Provider（Client 缓存 + 全局/按账号 Semaphore） |

### 关键常量

| 常量 | 行号 | 值 |
|------|------|-----|
| `MAX_RETRIES_PER_CREDENTIAL` | 28 | 3 |
| `MAX_TOTAL_RETRIES` | 31 | 9 |
| `MAX_CONCURRENT_REQUESTS` | 34 | 50（全局信号量） |
| `MAX_CONCURRENT_PER_CREDENTIAL` | 37 | 20（按账号信号量） |

均为硬编码常量，不支持环境变量覆盖。

### 关键函数

| 函数 | 行号 | 说明 |
|------|------|------|
| `KiroProvider::with_proxy` | 72 | 构造，预热全局代理 Client |
| `with_rpm_tracker` | 94 | builder：挂载 RPM 追踪器 |
| `with_throttle_log_store` | 100 | builder：挂载限流日志 |
| `client_for` | 112 | 按账号代理获取/创建 reqwest::Client |
| `semaphore_for` | 124 | 获取/创建该账号的按账号并发信号量 |
| `extract_model_from_request` | 187 | 从请求体提取 modelId |
| `extract_continuation_id_from_request` | 219 | 提取 agentContinuationId |
| `build_headers` | 232 | 构建 Kiro 请求头（machine_id / auth / agent_mode） |
| `build_mcp_headers` | 295 | 构建 MCP 请求头 |
| `call_api` | 353 | 非流式调用入口 |
| `call_api_stream` | 375 | 流式调用入口 |
| `call_mcp` | 393 | MCP 工具调用入口 |
| `call_mcp_with_retry` | 402 | MCP 重试逻辑 |
| `call_api_with_retry` | 607 | 重试逻辑（400/401/402/429/5xx 各自处理） |
| `retry_delay` | 872 | 指数退避 + 抖动 |
| `wait_for_rpm_gate` | 895 | 按账号 RPM 限速门（`maxRpmPerCredential`） |
| `is_monthly_request_limit` | 931 | 检测 402 MONTHLY_REQUEST_COUNT |
| `rewrite_profile_arn` | 959 | 企业版 IdC 请求体注入/改写 `profileArn` |

单账号最大重试次数 = `min(可用账号池大小 × MAX_RETRIES_PER_CREDENTIAL, MAX_TOTAL_RETRIES)`。

---

## 4. `src/kiro/machine_id.rs`

**职责**：设备指纹（machineId）生成，支持 UUID/64 位十六进制格式标准化。

| 函数 | 说明 |
|------|------|
| `normalize_machine_id` | 标准化格式：64 位十六进制直接返回；UUID 去连字符后补齐到 64 字符 |
| `generate_from_credentials` | 优先级：账号级 `machineId` → 全局 `config.machineId` → 用 `refreshToken` 做 SHA-256 派生 |

---

## 5. `src/kiro/parser/`

**职责**：Kiro 私有二进制帧协议（AWS Event Stream）解码，含 CRC32 校验。

### 文件清单

| 文件 | 行号 | 说明 |
|------|------|------|
| `mod.rs` | — | 子模块声明与重导出（11 行） |
| `crc.rs` | 18 | `pub fn crc32(data: &[u8]) -> u32`（CRC32 ISO-HDLC） |
| `error.rs` | 8 | `pub enum ParseError`；`pub type ParseResult<T>`（95） |
| `frame.rs` | 35 | `pub struct Frame`；`pub fn parse_frame` (76)；`PRELUDE_SIZE=12` (25)；`MIN_MESSAGE_SIZE=PRELUDE_SIZE+4` (28) |
| `header.rs` | — | `pub enum HeaderValueType` (14)；`pub enum HeaderValue` (51)（两个独立枚举，非单一枚举）；`pub struct Headers` (75)；`pub fn parse_headers` (131) |
| `decoder.rs` | — | `pub enum DecoderState`（4 态，55）；`pub struct EventStreamDecoder`（88） |

### `EventStreamDecoder` 关键方法

| 方法 | 行号 | 说明 |
|------|------|------|
| `new` / `with_capacity` / `with_config` | 114 / 119 / 132 | 构造 |
| `feed` | 149 | 追加待解码字节 |
| `decode` | 175 | 解出一帧；出错时内部调用 `try_recover` 并转入 `Recovering` 状态 |
| `decode_iter` | 236 | 返回 `DecodeIter` 迭代器 |
| `try_recover`（私有） | 245 | `decode` 出错时的字节级容错恢复：Prelude 阶段错误跳 1 字节找下一帧边界，Data 阶段错误跳过整帧 |
| `try_resume`（公开） | 367 | 供外部调用：将 `Stopped` 状态的解码器重置错误计数并转回 `Ready`（缓冲区内容保留） |
| `reset` / `state` / `is_ready` / `is_stopped` / `is_recovering` | 315–339 | 状态查询/重置 |
| `frames_decoded` / `error_count` / `bytes_skipped` / `buffer_len` | 344–360 | 统计查询 |

> `try_recover` 与 `try_resume` 是两个独立方法，不是改名关系：前者是 `decode()` 内部自动触发的字节级恢复，后者是暴露给调用方在解码器彻底 `Stopped` 后手动恢复的公开 API。

---

## 6. `src/kiro/model/`

**职责**：Kiro 上游协议的请求/响应数据模型（凭证、事件、请求体）。

| 子模块 | 说明 |
|--------|------|
| `credentials.rs`（949 行） | `pub struct KiroCredentials`（17，字段全部 `Option`，除 `priority: u32` 和 `disabled: bool`）；`pub enum CredentialsConfig`（166，单账号/多账号两种反序列化格式） |
| `token_refresh.rs`（45 行） | `RefreshRequest`/`RefreshResponse`（social）、`IdcRefreshRequest`/`IdcRefreshResponse`（企业版 IdC） |
| `usage_limits.rs`（203 行） | `UsageLimitsResponse`、`SubscriptionInfo`、`UsageBreakdown`、`Bonus`、`FreeTrialInfo` — 账号余额/额度查询响应结构 |
| `common/mod.rs` | 空壁架文件（5 行，暂无内容） |
| `events/`（见下） | Kiro 流式响应的二进制帧事件 payload |
| `requests/`（见下） | 发往 Kiro 的请求体结构 |

### `events/`

| 文件 | 说明 |
|------|------|
| `base.rs` | `pub enum EventType`（11）；`pub enum Event`（70，各事件类型的统一封装） |
| `assistant.rs` | `pub struct AssistantResponseEvent`（33，助手文本增量） |
| `code_reference.rs` | `pub struct CodeReference`（12）+ `CodeReferenceEvent`（30） |
| `context_usage.rs` | `pub struct ContextUsageEvent`（18，上下文占用百分比） |
| `metering.rs` | `pub struct MeteringEvent`（12，计费真值：可能含 `cache_read_input_tokens`/`cache_creation_input_tokens`） |
| `tool_use.rs` | `pub struct ToolUseEvent`（18，工具调用增量） |
| `mod.rs` | 统一 `pub use` 重导出以上事件类型 |

### `requests/`

| 文件 | 说明 |
|------|------|
| `conversation.rs`（407 行） | `ConversationState`（15）、`CurrentMessage`（81）、`UserInputMessage`（96）、`UserInputMessageContext`（147）、`KiroImage`/`KiroImageSource`（174/193）、`Message` enum（203）、历史消息结构（`HistoryUserMessage`/`UserMessage`/`HistoryAssistantMessage`/`AssistantMessage`） |
| `kiro.rs`（72 行） | `pub struct KiroRequest`（33，发往 Kiro 的顶层请求体） |
| `tool.rs`（193 行） | `Tool`（13）、`ToolSpecification`（23）、`InputSchema`（36）、`ToolResult`（64）、`ToolUseEntry`（120） |

---

## 7. `src/anthropic/router.rs`

**职责**：注册 `/v1` 与 `/cc/v1` 两组路由并分别挂载认证中间件、CORS、200MB 请求体上限、`TraceLayer`。

| 内容 | 行号 | 说明 |
|------|------|------|
| `MAX_BODY_SIZE` | 20 | 200MB |
| `create_router_with_provider_and_state` | 39 | 对外入口：可选注入 `KiroProvider`/`profile_arn` 后调用 `build_router` |
| `build_router`（私有） | 53 | 组装 `public_routes`（`/v1/ping`，免认证）+ `v1_routes` + `cc_v1_routes`（均挂 `auth_middleware`） |

`/v1` 与 `/cc/v1` 均注册 `POST /messages`、`POST /messages/count_tokens`；`/v1` 额外注册 `GET /models`、`GET /models/{model_id}`。两者路由入口函数不同（`post_messages` vs `post_messages_cc`），差异见 `handlers.rs` 一节。

---

## 8. `src/anthropic/handlers.rs`

**职责**：Anthropic 兼容 API 的 HTTP 处理函数。

| 函数 | 行号 | 路由/用途 |
|------|------|------|
| `ping` | 37 | `GET /v1/ping` |
| `map_provider_error_with_context` | 66 | 内部：错误映射（429→429，上下文超限→400） |
| `get_models` | 177 | `GET /v1/models` |
| `build_model_list` | 187 | 内部：构建模型列表（新老模型 ID） |
| `get_model` | 479 | `GET /v1/models/:id` |
| `post_messages` | 501 | `POST /v1/messages`（直通流式） |
| `handle_stream_request` | 711 | 内部：流式响应处理 |
| `create_sse_stream` | 788 | 内部：SSE 事件流（含 25s ping 保活） |
| `handle_non_stream_request` | 908 | 内部：非流式响应（含事件解码） |
| `override_thinking_from_model_name` | 1236 | 内部：从模型名后缀注入 thinking 配置 |
| `count_tokens` | 1276 | `POST /v1/messages/count_tokens` |
| `post_messages_cc` | 1302 | `POST /cc/v1/messages`（缓冲模式） |
| `handle_stream_request_buffered` | 1508 | 内部：`/cc/v1` 专用缓冲流处理（等待上游流完成后再发送） |
| `create_buffered_sse_stream` | 1553 | 内部：缓冲版 SSE 流 |

---

## 9. `src/anthropic/converter.rs`

**职责**：Anthropic → Kiro 请求格式转换（模型映射、消息历史构建、工具 schema 规范化）。

| 函数/类型 | 行号 | 说明 |
|-----------|------|------|
| `normalize_json_schema` | 28 | 修复 MCP 工具 schema（null 字段、类型合并），内部委托 `normalize_json_schema_inner`（114） |
| `normalize_billing_header` | 397 | 固定 cch= 值，确保 prompt cache 命中 |
| `map_model` | 420 | Anthropic 模型名 → Kiro 模型 ID |
| `pub struct ConversionResult` | 467 | 转换结果（含 conversation_state） |
| `pub enum ConversionError` | 476 | UnsupportedModel / EmptyMessages |
| `extract_session_id` | 504 | 从 metadata.user_id 提取 session UUID |
| `derive_agent_continuation_id` | 535 | SHA-256 派生 agentContinuationId（sticky 路由依据） |
| `convert_request` | 646 | **主入口**：完整 Anthropic→Kiro 转换 |
| `determine_agent_task_type` | 803 | 根据工具名判断 vibe/spectask |
| `validate_tool_pairing` | 1175 | 验证 tool_use/tool_result 配对完整性 |
| `remove_orphaned_tool_uses` | 1256 | 清理孤立 tool_use |
| `convert_tools` | 1285 | 转换工具定义（描述截断、schema 规范化） |
| `build_history` | 1439 | 构建历史消息（系统消息注入、thinking 前缀） |

> 本文件共 3072 行、44 个顶层声明；上表仅覆盖公开/核心入口，其余为内部私有辅助函数（未逐一列出）。

---

## 10. `src/anthropic/stream.rs`

**职责**：Kiro 事件流 → Anthropic SSE 转换，管理 message_start/content_block/tool_use 状态机。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `find_real_thinking_end_tag` | 69 | 识别真正的 thinking 结束标签（过滤引用场景） |
| `find_real_thinking_end_tag_at_buffer_end` | 116 | 识别缓冲区末尾处的 thinking 结束标签（跨 chunk 边界场景） |
| `find_real_thinking_start_tag` | 149 | 识别真正的 thinking 开始标签 |
| `pub struct SseEvent` | 177 | SSE 事件（event + data） |
| `SseEvent::to_sse_string` | 191 | 格式化为 `event:\ndata:\n\n` |
| `pub struct SseStateManager` | 226 | SSE 状态机（防重复 message_start，tool_use 自动关闭文本块） |
| `SseStateManager::get_stop_reason` | 299 | tool_use 优先的 stop_reason |
| `context_window_for_model` | 548 | `pub(crate) fn(_model: &str) -> i32`，**对所有模型统一返回 1,000,000**，不再按模型分档 |
| `pub struct StreamContext` | 616 | 流式处理上下文（用量追踪、事件处理） |
| `pub struct BufferedStreamContext` | 1534 | 缓冲版上下文（cc 端点，延迟 message_start） |
| `estimate_tokens` | 1696 | 输出 token 数估算（直接迭代 chars，不做堆分配） |

---

## 11. `src/anthropic/middleware.rs`

**职责**：Axum 中间件层：API Key 认证、CORS 配置、AppState 定义。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct ApiKeyContext` | 25 | 认证后注入 Extension（id + bound_credential_ids） |
| `pub struct AppState` | 34 | 应用共享状态（api_key / kiro_provider / rpm_tracker / usage_tracker 等） |
| `AppState::new` | 53 | 构造（只需 api_key） |
| `with_kiro_provider` / `with_profile_arn` / `with_api_key_manager` / `with_usage_tracker` / `with_rpm_tracker` | 66 / 72 / 78 / 84 / 90 | Builder 链式设置各字段 |
| `with_fingerprint_tracker` | 96 | Builder：挂载 `FingerprintTracker`（prompt cache 指纹追踪，见 §21） |
| `auth_middleware` | 111 | 认证中间件（主密钥 → 子 API Key → 401） |
| `cors_layer` | 219 | 允许所有来源的 CORS 层 |

---

## 12. `src/anthropic/types.rs`

**职责**：Anthropic API 所有请求/响应类型定义。

| 类型 | 行号 | 说明 |
|------|------|------|
| `pub struct ErrorResponse` | 11 | `{ error: { type, message } }` |
| `pub struct Model` | 44 | 模型信息（id / display_name / created_at） |
| `pub struct Thinking` | 69 | thinking 配置（type + budget_tokens） |
| `pub struct MessagesRequest` | 128 | 主请求体（model / messages / system / tools / stream / thinking 等） |
| `pub struct Message` | 204 | role + content（Value） |
| `pub struct Tool` | 222 | 工具定义（含 WebSearch type） |
| `pub struct ContentBlock` | 247 | text / image / tool_use / tool_result 等 |
| `pub struct CountTokensRequest` | 283 | count_tokens 请求体 |
| `pub struct CountTokensResponse` | 298 | `{ input_tokens }` |

---

## 13. `src/anthropic/websearch.rs`

**职责**：内置 Web Search 工具支持——检测请求中的 web_search 工具，转发给 Kiro 的 MCP 接口，并把结果包装成 Anthropic SSE 事件流。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `McpRequest` / `McpParams` / `McpArguments` | 24 / 33 / 40 | 发往 Kiro MCP 接口的请求结构 |
| `McpResponse` / `McpError` / `McpResult` / `McpContent` | 47 / 56 / 64 / 72 | MCP 响应结构 |
| `WebSearchResults` / `WebSearchResult` | 81 / 92 | 解析后的搜索结果结构 |
| `has_web_search_tool` | 109 | 检测 `MessagesRequest.tools` 中是否含 web_search 工具 |
| `extract_search_query` | 119 | 从消息中提取搜索查询词 |
| `create_mcp_request` | 172 | 构建发往 Kiro 的 MCP 请求 |
| `parse_search_results` | 204 | 解析 MCP 响应为 `WebSearchResults` |
| `create_websearch_sse_stream` | 216 | 将搜索结果包装为 Anthropic SSE 事件流 |
| `handle_websearch_request` | 443 | 处理入口：串联检测→调用→解析→SSE 输出 |

---

## 14. `src/model/config.rs`

**职责**：应用配置结构定义、文件加载、环境变量覆盖。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub enum TlsBackend` | 11 | Rustls / NativeTls |
| `pub struct Config` | 73 | 全量配置（host / port / region / auth_region / api_region / proxy / admin_api_key / load_balancing_mode / cache_simulation 等） |
| `Config::effective_auth_region` | 235 | auth_region → region 回退 |
| `Config::effective_api_region` | 241 | api_region → region 回退 |
| `Config::load` | 246 | 从 JSON 文件加载，不存在返回默认值 |
| `Config::apply_env_overrides` | 294 | 环境变量覆盖（API_KEY / HOST / PORT / REGION 等） |

---

## 15. `src/model/rpm.rs`

**职责**：滑动窗口 RPM 统计（全局 / 按账号 / 按 API Key 三维度）。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `struct TimestampQueue` | 23 | 时间戳滑动窗口队列 |
| `pub struct RpmTracker` | 75 | 线程安全追踪器 |
| `pub struct RpmSnapshot` | 91 | 快照（global + by_credential + by_api_key） |
| `RpmTracker::record_request` | 115 | 记录全局 + per-key RPM |
| `RpmTracker::record_credential` | 132 | 记录 per-credential RPM |
| `RpmTracker::snapshot` | 178 | 返回当前快照 |

---

## 16. `src/model/throttle_log.rs`

**职责**：记录 429 限流事件并持久化到 `throttle_log.json`，支持分页查询。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct ThrottleEvent` | 17 | 单条事件（credential_id / request_type / status_code / response_body / created_at） |
| `pub struct ThrottleLogStore` | 31 | 存储（RwLock + 文件路径） |
| `ThrottleLogStore::load` | 48 | 从文件加载 |
| `ThrottleLogStore::record` | 123 | 追加事件，按 credential 裁剪上限（500 条） |
| `ThrottleLogStore::get_paged` | 179 | 分页查询（created_at 降序） |
| `pub struct ThrottleLogsPage` | 243 | 分页结果（records / total / page / page_size / total_pages） |

---

## 17. `src/model/api_key.rs`

**职责**：API Key 的 CRUD、认证、懒激活模式、额度检查。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct ApiKey` | 11 | Key 实体（id / key / name / enabled / expires_at / spending_limit / duration_days / activated_at / bound_credential_ids） |
| `ApiKey::is_expired` | 92 | 过期检测（含懒激活状态） |
| `ApiKey::activate` | 108 | 首次使用激活，计算 expires_at |
| `pub enum ApiKeyAuthResult` | 127 | Valid / Disabled / Expired / NotFound |
| `pub struct ApiKeyManager` | 145 | 管理器（RwLock + 文件路径） |
| `ApiKeyManager::authenticate` | 182 | 认证（检查 enabled + expired） |
| `ApiKeyManager::authenticate_readonly` | 206 | 只读认证（不检查状态，供用量查询） |
| `ApiKeyManager::create` | 226 | 创建新 key |
| `ApiKeyManager::update` | 254 | 更新 key（含续期逻辑） |
| `ApiKeyManager::activate_key` | 341 | 激活指定 key（幂等） |

---

## 18. `src/model/usage.rs`

**职责**：记录每个 API Key 的 token 用量、按 `get_k_ref` 计费系数估算费用并持久化。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct UsageRecord` | 19 | 单条记录（api_key_id / credential_id / model / input_tokens / output_tokens / cost / created_at） |
| `pub struct UsageSummary` | 58 | 汇总（total_requests / tokens / cost，按模型分组） |
| `get_k_ref`（私有） | 124 | 计费系数：opus-4.7/opus-4.8/opus-5/未知 opus·fable 兜底 = 2.36；opus-4.5/opus-4.6 = 1.90；sonnet 系列/sonnet-5/haiku 默认档 = 1.43 |
| `pub struct UsageTracker` | 160 | 追踪器（RwLock + 文件路径） |
| `UsageTracker::record` | 246 | 记录用量并持久化 |
| `UsageTracker::get_summary` | 319 | 返回指定 key 的汇总 |
| `UsageTracker::get_total_cost` | 393 | 返回累计费用（供额度检查） |
| `pub struct UsageRecordsPage` | 506 | 分页结果 |
| `pub struct UsageRecordItem` | 517 | 分页单条记录 DTO |

---

## 19. `src/model/arg.rs`

**职责**：`clap` 命令行参数定义。

| 类型 | 说明 |
|------|------|
| `pub struct Args` | `--config` / `-c`（配置文件路径）、`--credentials`（凭证文件路径），均为 `Option<String>` |

---

## 20. `src/model/failure_log.rs`

**职责**：记录每个账号收到 401/403 响应的失败事件，持久化到 `failure_log.json`，供 Admin 面板排障。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct FailureEvent` | 17 | 单条失败事件（credential_id / request_type / status_code / response_body 截断 2000 字符 / created_at） |
| `MAX_EVENTS_PER_CREDENTIAL` | 28 | 500（每账号最大保留条数） |
| `pub struct FailureLogStore` | 31 | 存储（RwLock + 异步落盘队列） |
| `pub struct FailureLogsPage` | 243 | 分页结果 |
| `pub struct FailureLogItem` | 254 | 分页单条记录 DTO |

---

## 21. `src/cache/`

**职责**：Prompt Cache 用量确定 —— 四层降级链 + 账号级前缀指纹追踪。取代旧的单文件 `src/cache.rs`。

### `mod.rs`（207 行）

| 函数 | 行号 | 说明 |
|------|------|------|
| `select_final_usage` | 33 | **四层降级链主入口**：① `metering` 真值 → ② `prefix_estimated_read`（`token::count_prefix_tokens` 前缀估算） → ③ `fingerprint_usage`（账号级指纹命中） → ④ `ratio_fallback`（比例模拟）。所有分支输出经 `clamp_to_total` 截断保证 5m/1h 拆分不变性。 |

### `simulation.rs`（476 行，原 `cache.rs` 内容迁移至此）

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct CacheSimulationRatioConfig` | 23 | 配置（min / max / peak_ratio / focus_radius / focus_probability） |
| `CacheSimulationRatioConfig::fixed` | 32 | 固定比例快捷构造 |
| `pub struct PromptCacheUsage` | 176 | 模拟的 cache 用量（input_tokens / cache_creation / cache_read / 5m·1h 拆分） |
| `split_creation_by_ephemeral_ratio` | 185 | 按 1h 占比拆分 cache_creation 为 5m/1h 两档 |
| `PromptCacheUsage::from_ratio_config` / `from_ratio_config_with_ephemeral` | 237 / 250 | 从比例配置采样生成 |
| `PromptCacheUsage::scale_to` | 270 | 按实际 input_tokens 重新缩放 |
| `PromptCacheUsage::clamp_to_total` | 308 | 截断到 total_input，保证不变性 |

### `fingerprint.rs`（877 行）— 账号级前缀指纹追踪

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `CACHE_READ_CAP_RATIO`（私有常量） | 63 | 0.85 —— cache_read 上限 = `min(匹配到的累计 token 数, 0.85 × total_input)` |
| `pub enum EphemeralTier` | 31 | 5m / 1h 档位 |
| `struct Breakpoint` | 37 | 单个前缀断点（累计 hash + token 数 + 命中时间） |
| `struct FingerprintTable` | 46 | 单账号的断点集合 |
| `struct ContentSegment` | 51 | 规范化后的单段内容（system/message），用于计算累计 hash |
| `pub struct FingerprintTracker` | 57 | 追踪器主体 |
| `FingerprintTracker::build_profile` / `build_profile_with_tools` | 107 / 116 | 将请求分段并规范化为 `Vec<ContentSegment>`（trim 文本、`tool_use.input` JSON key 排序、图片 `source.data` 短哈希） |
| `FingerprintTracker::compute` | 182 | 计算累计 SHA-256（`hash[k] = SHA-256(seg[0]‖...‖seg[k])`，前缀单调）并与账号断点表匹配，按 `CACHE_READ_CAP_RATIO` 截断 |
| `FingerprintTracker::update` | 240 | 写入/更新账号断点表 |
| `FingerprintTracker::evict_expired` | 294 | TTL 淘汰（按 `CacheSimulationConfig` 的 `fingerprint_ttl_5m`/`fingerprint_ttl_1h`）；容量淘汰只裁剪尾部，不按 `last_hit_at` 重排（重排会破坏累计 hash 链的前缀单调性，导致整表命中率清零） |

> 与 `docs/Sub2API缓存机制.md` 中提到的 `fingerprintUnification`（HTTP 头指纹伪装，防止上游识别客户端）是完全不同的概念，本模块只做 prompt 内容前缀匹配。详见 `docs/Kiro缓存与状态管理解析.md` §6.3.1。

---

## 22. `src/admin/`

### `router.rs`（83 行）

| 函数 | 行号 | 说明 |
|------|------|------|
| `create_admin_router` | 27 | 组装挂载 `admin_auth_middleware` 的受保护路由（`protected`）+ 独立的日志流路由（`log_routes`），返回合并后的 Router |

路由前缀 `/api/admin`（部分列举，完整列表见 router.rs 源码）：

| 路由 | 说明 |
|------|------|
| `GET/POST /credentials` | 账号列表 / 新增 |
| `PUT/DELETE /credentials/{id}` | 更新 / 删除 |
| `POST /credentials/{id}/disabled\|priority\|reset` | 禁用 / 优先级 / 重置 |
| `GET /credentials/{id}/balance\|throttle-logs\|failure-logs` | 余额 / 限流日志 / 失败日志 |
| `GET /credentials/{id}/usage/records`、`GET /credentials/{id}/today-summary` | 单账号用量分页 / 今日汇总 |
| `GET/PUT /config/load-balancing` | 负载均衡模式 |
| `GET/PUT /config/auth-keys` | Admin/API 认证密钥配置 |
| `GET/POST /api-keys`，`PUT/DELETE /api-keys/{id}` | API Key 列表/创建/更新/删除 |
| `GET /api-keys/usage`、`GET /api-keys/{id}/usage/records` | 用量汇总 / 分页明细 |
| `GET /rpm` | RPM 快照 |
| `GET /usage/daily`、`GET /usage/daily/{date}/records` | 每日用量汇总 / 明细 |
| `GET /server-info` | 服务信息 |
| `GET /logs/stream\|snapshot\|download` | 日志流（SSE）/ 快照 / 下载 |

### `handlers.rs`（240 行）— 账号 CRUD + 负载均衡 + 认证密钥

| 函数 | 行号 | 路由 |
|------|------|------|
| `get_all_credentials` | 20 | `GET /credentials` |
| `set_credential_disabled` | 27 | `POST /credentials/:id/disabled` |
| `set_credential_priority` | 43 | `POST /credentials/:id/priority` |
| `reset_failure_count` | 60 | `POST /credentials/:id/reset` |
| `get_credential_balance` | 76 | `GET /credentials/:id/balance` |
| `add_credential` | 88 | `POST /credentials` |
| `delete_credential` | 100 | `DELETE /credentials/:id` |
| `update_credential` | 112 | `PUT /credentials/:id` |
| `get_load_balancing_mode` / `set_load_balancing_mode` | 125 / 132 | `GET/PUT /config/load-balancing` |
| `get_auth_keys` / `set_auth_keys` | 151 / 167 | `GET/PUT /config/auth-keys` |

### `api_keys.rs`（333 行）— API Key、用量、RPM、日志查询

| 函数 | 行号 | 路由 |
|------|------|------|
| `get_server_info` | 19 | `GET /server-info` |
| `list_api_keys` / `create_api_key` | 34 / 46 | `GET/POST /api-keys` |
| `update_api_key` / `delete_api_key` | 73 / 107 | `PUT/DELETE /api-keys/:id` |
| `get_all_usage` | 131 | `GET /api-keys/usage` |
| `get_key_usage` / `reset_key_usage` | 141 / 154 | 单 key 用量查询/重置 |
| `get_key_usage_records` | 173 | `GET /api-keys/:id/usage/records`（分页） |
| `get_credential_usage_records` / `get_credential_today_summary` | 197 / 221 | 单账号用量分页 / 今日汇总 |
| `get_rpm` | 234 | `GET /rpm` |
| `get_daily_usage` / `get_daily_usage_records` | 258 / 268 | `GET /usage/daily`、`GET /usage/daily/:date/records` |
| `get_failure_logs` / `get_throttle_logs` | 291 / 314 | 单账号失败日志 / 限流日志分页 |

### `service.rs`（484 行）— 业务逻辑层，委托 `MultiTokenManager`

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `struct CachedBalance` | 27 | 余额缓存条目（含 cached_at） |
| `pub struct AdminService` | 37 | 服务结构，持有 `Arc<MultiTokenManager>` |
| `AdminService::new` | 44 | 构造 |
| `get_all_credentials` | 59 | 组装 `CredentialsStatusResponse` |
| `set_disabled` / `set_priority` / `reset_and_enable` | 98 / 115 / 122 | 委托 `MultiTokenManager` 对应方法 |
| `get_balance` | 129 | 带缓存的余额查询（TTL 5min） |
| `add_credential` / `delete_credential` / `update_credential` | 190 / 243 / 259 | 账号 CRUD |
| `credential_labels` | 280 | 返回 `id → 标签` 映射，供日志/用量展示 |
| `get_load_balancing_mode` / `set_load_balancing_mode` | 297 / 304 | 负载均衡模式读写 |
| `sticky_metrics` | 464 | 返回 sticky 路由命中/未命中计数 |

### `error.rs`（65 行）

| 类型 | 行号 | 说明 |
|------|------|------|
| `pub enum AdminServiceError` | 12 | Service 层统一错误类型，实现到 HTTP 状态码的映射 |

### `middleware.rs`（125 行）

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct AdminState` | 28 | Admin 路由共享状态（token_manager / api_key_manager / usage_tracker 等） |
| `admin_auth_middleware` | 109 | 校验 `adminApiKey`；未配置该字段时 Admin API 整体不可用 |

### `log_handler.rs`（164 行）— 依托 `src/log_capture.rs` 的 ring buffer

| 函数 | 行号 | 说明 |
|------|------|------|
| `stream_logs` | 30 | `GET /logs/stream`，SSE 实时推送新日志（`LogCapture::subscribe`） |
| `snapshot_logs` | 91 | `GET /logs/snapshot`，返回当前 ring buffer 全量快照 |
| `download_logs` | 120 | `GET /logs/download`，导出日志为文件下载 |

### `types.rs`（402 行）— Admin API 全部 DTO

| 类型 | 行号 | 说明 |
|------|------|------|
| `CredentialsStatusResponse` | 11 | 账号列表响应 |
| `CredentialStatusItem` | 25 | 单账号状态（含 health_status / throttle_count） |
| `SetDisabledRequest` / `SetPriorityRequest` | 67 / 75 | `{ disabled }` / `{ priority }` |
| `AddCredentialRequest` / `AddCredentialResponse` | 83 / 141 | 新增账号请求/响应 |
| `UpdateCredentialRequest` | 154 | 更新请求（patch 语义，全可选） |
| `BalanceResponse` | 200 | 余额响应 |
| `LoadBalancingModeResponse` / `SetLoadBalancingModeRequest` | 222 / 230 | 负载均衡模式读/写 |
| `SuccessResponse` | 239 | 通用成功响应 |
| `CreateApiKeyRequest` / `UpdateApiKeyRequest` | 258 / 281 | API Key 创建/更新请求 |
| `AdminErrorResponse` / `AdminError` | 338 / 343 | 统一错误响应体 |
| `AuthKeysResponse` / `SetAuthKeysRequest` | 385 / 395 | 认证密钥读/写 |

---

## 23. `src/admin_ui/`

**职责**：`rust-embed` 嵌入 `admin-ui/dist` 前端构建产物，提供 SPA 静态资源路由。

| 内容 | 行号 | 说明 |
|------|------|------|
| `#[derive(Embed)] struct Asset`（`#[folder = "admin-ui/dist"]`） | 14 | 编译期嵌入前端构建产物 |
| `create_admin_ui_router` | 19 | 路由：`/` → `index_handler`；`/{*file}` → `static_handler`（拒绝含 `..` 的路径，未匹配文件回退 `index.html` 实现 SPA 路由） |

`mod.rs` 仅 8 行，只做子模块声明。

---

## 24. `src/user/`

**职责**：面向子 API Key 持有者的自助查询 API（登录校验、用量查询）。

### `handlers.rs`（161 行）

| 函数 | 行号 | 路由 |
|------|------|------|
| `login` | 13 | `POST /api/user/login` |
| `get_usage` | 73 | `GET /api/user/usage` |
| `get_usage_records` | 103 | `GET /api/user/usage/records`（分页明细） |

DTO：`LoginRequest`（127）、`LoginResponse`（133）、`UsageResponse`（147）。

### `middleware.rs`（98 行）

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct UserState` | 21 | 共享状态（api_key_manager / usage_tracker） |
| `pub struct UserContext` | 28 | 认证后注入 Extension |
| `pub struct UserErrorResponse` | 37 | 统一错误响应体 |
| `user_auth_middleware` | 43 | 校验子 API Key（`authenticate_readonly`，不检查启用/过期状态） |

### `router.rs`（32 行）

| 路由 | 处理函数 |
|------|----------|
| `POST /login` | `login` |
| `GET /usage` | `get_usage` |
| `GET /usage/records` | `get_usage_records` |

`create_user_router(state: UserState) -> Router`（15）为对外构造函数。

---

## 25. `src/user_ui/`

**职责**：`rust-embed` 嵌入 `user-ui/dist` 前端构建产物，结构与 `src/admin_ui/` 完全对称。

| 内容 | 说明 |
|------|------|
| `create_user_ui_router` | 路由：`/` + `/{*file}`，逻辑与 `admin_ui::create_admin_ui_router` 一致，仅嵌入目录换成 `user-ui/dist` |

---

## 26. `src/common/auth.rs`

**职责**：认证工具函数（从请求提取 API Key、防时序攻击比较）。

| 函数 | 行号 | 说明 |
|------|------|------|
| `extract_api_key` | 15 | 从 `x-api-key` 或 `Authorization: Bearer` 提取 key |
| `constant_time_eq` | 40 | `subtle` crate 防时序攻击比较 |

---

## 27. `src/http_client.rs`

**职责**：统一构建 reqwest::Client，支持代理、TLS 后端切换、连接池配置。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct ProxyConfig` | 13 | 代理配置（url + 可选认证） |
| `ProxyConfig::new` | 24 | 从 URL 构造 |
| `ProxyConfig::with_auth` | 33 | 链式设置用户名密码 |
| `pub fn build_client` | 48 | 构建 reqwest::Client（超时 / 连接池 / TLS / 代理） |

---

## 28. `src/token.rs`

**职责**：Token 数量估算（本地四分类加权 heuristic）和可选的外部 count_tokens API 调用。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct CountTokensConfig` | 19 | count_tokens 配置（api_url / key / auth_type / proxy） |
| `pub fn init_config` | 39 | 初始化全局配置（OnceLock，只能调用一次） |
| `pub fn count_tokens` | 75 | 本地四分类加权估算（见下） |
| `pub(crate) fn count_all_tokens` | 101 | 汇总 system+messages+tools token 数：优先调远程 API，失败回退本地计算 |
| `count_content_block`（私有） | 187 | 单个 content block 计数（text / tool_use / tool_result） |
| `count_all_tokens_local`（私有） | 228 | 本地汇总实现 |

### `count_tokens` 算法（第 75 行起，四分类加权，向上取整、最少 1）

```
ASCII 字母 (A-Za-z)         → 每字符 / 4.5   （英文 BPE 平均 ~4.5 chars/token）
数字 (0-9)                  → 每字符 / 2.0   （数字 BPE 拆分粒度细）
其他 ASCII（符号/空白）      → 每字符 / 1.5   （符号常单独成 token）
非 ASCII（CJK 等）           → 每字符 / 1.5   （中文 BPE 平均 ~1.5 chars/token）
```

> 旧的 `is_non_western_char` 判定函数仍保留在文件中（`#[allow(dead_code)]`），但已不被 `count_tokens` 使用，属于预置死代码。

---

## 29. `src/debug.rs`

**职责**：CLI 调试辅助函数，人类可读方式打印二进制帧/事件（`RUST_LOG=debug` 场景下配合日志使用）。

| 函数 | 行号 | 说明 |
|------|------|------|
| `print_hex` | 10 | 十六进制 + ASCII 双栏打印字节数据 |
| `debug_crc` | 47 | 打印帧的 CRC 校验计算过程 |
| `print_frame_summary` | 100 | 打印单帧摘要（类型、长度、头部） |
| `print_event_verbose` | 126 | 打印 `Event` 完整字段 |
| `print_event` | 181 | 打印 `Event` 精简摘要 |

---

## 30. `src/log_capture.rs`

**职责**：内存日志捕获层 —— 作为 `tracing_subscriber::Layer` 接入，把日志事件写入 ring buffer 并通过 `broadcast` 通道广播，供 Admin 面板日志页面（`src/admin/log_handler.rs`）实时展示/下载。

| 类型/函数 | 行号 | 说明 |
|-----------|------|------|
| `pub struct LogEntry` | 17 | 单条日志（timestamp / level / target / message） |
| `pub struct LogCapture` | 24 | 持有 ring buffer + broadcast sender，`new`/`as_layer`/`snapshot`/`subscribe` |
| `pub struct LogCaptureLayer` | 59 | 实现 `tracing_subscriber::Layer`，`on_event` 写入 ring buffer 并广播 |

---

## 快速功能定位

| 要找什么 | 去哪里 |
|---------|--------|
| 账号选择 / 故障转移 | `token_manager.rs:acquire_context` (926) |
| 健康状态计算 | `token_manager.rs:compute_health` (1476)，非时间窗口模型，见 §2 算法说明 |
| 限流事件记录 | `token_manager.rs:report_throttled` (1520) |
| 请求格式转换 | `converter.rs:convert_request` (646) |
| 模型名映射 | `converter.rs:map_model` (420) |
| tool schema 修复 | `converter.rs:normalize_json_schema` (28) |
| SSE 事件转换 | `stream.rs:SseStateManager` (226) |
| thinking 标签处理 | `stream.rs:find_real_thinking_end_tag` (69) |
| HTTP 重试逻辑 | `provider.rs:call_api_with_retry` (607) |
| 并发限流（全局/按账号） | `provider.rs:MAX_CONCURRENT_REQUESTS` (34) / `MAX_CONCURRENT_PER_CREDENTIAL` (37) |
| 二进制帧解码 | `parser/decoder.rs:EventStreamDecoder`（`decode` 175 / `try_resume` 367） |
| 认证中间件 | `middleware.rs:auth_middleware` (111) |
| API Key 管理 | `model/api_key.rs:ApiKeyManager` |
| 用量统计 / 计费系数 | `model/usage.rs:UsageTracker`；`get_k_ref` (124) |
| RPM 追踪 | `model/rpm.rs:RpmTracker` |
| 配置加载 / 环境变量 | `model/config.rs:Config::apply_env_overrides` (294) |
| Admin REST API 路由 | `admin/router.rs:create_admin_router` (27) |
| Admin 日志流 | `admin/log_handler.rs:stream_logs` (30) + `log_capture.rs:LogCapture` |
| Token 估算 | `token.rs:count_tokens` (75)（四分类加权：字母/4.5、数字/2.0、ASCII 符号/1.5、CJK/1.5） |
| Prompt cache 四层降级链 | `cache/mod.rs:select_final_usage` (33) |
| Prompt cache 指纹追踪 | `cache/fingerprint.rs:FingerprintTracker`（`compute` 182 / `update` 240 / `evict_expired` 294） |
| ephemeral 5m/1h 拆分 | `cache/simulation.rs:PromptCacheUsage::cache_creation_5m/1h_input_tokens`；JSON 输出 `usage.cache_creation.{ephemeral_5m, ephemeral_1h}_input_tokens` |
| contextUsage 本地化 | `stream.rs::context_window_for_model` 统一返回 1M；`Event::ContextUsage` 仅保留 100% stop_reason 兜底 |
| Web Search 内置工具 | `anthropic/websearch.rs:handle_websearch_request` (443) |
| 设备指纹（machineId）生成 | `kiro/machine_id.rs:generate_from_credentials` |
| 账号失败日志（401/403） | `model/failure_log.rs:FailureLogStore` |
